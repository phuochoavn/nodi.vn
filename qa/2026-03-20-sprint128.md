# QA Report — Sprint 128 (DB Role + Nginx Rate Limiting)

**Date:** 2026-03-20  
**QA Agent:** Antigravity AI  
**Server:** VPS (nodi production)  
**Overall Result:** ✅ ALL CHECKS PASSED (12/12)

---

## Part A — DB Role (#9)

### Test 1: Role `nodi_app` tồn tại với quyền hạn chế ✅ PASS

```
Command: SELECT rolname, rolsuper, rolcreatedb, rolcreaterole FROM pg_roles
         WHERE rolname IN ('nodi_app','nodi_admin');

Result:
  rolname    | rolsuper | rolcreatedb | rolcreaterole
 ------------+----------+-------------+---------------
  nodi_admin | t        | t           | t
  nodi_app   | f        | f           | f
```

**Verdict:** `nodi_app` có rolsuper=f, rolcreatedb=f, rolcreaterole=f — đúng yêu cầu.

---

### Test 2: API đang dùng `nodi_app` (không phải `nodi_admin`) ✅ PASS

```
Command: SELECT usename, application_name FROM pg_stat_activity
         WHERE datname='nodi' AND usename != 'nodi_admin';

Result:
 usename  | application_name
----------+------------------
 nodi_app |
 nodi_app |
(2 rows)
```

**Verdict:** API đang kết nối DB qua user `nodi_app` — 2 connection active.

---

### Test 3: `nodi_admin` vẫn tồn tại và vẫn superuser ✅ PASS

```
Result: nodi_admin | rolsuper=t, rolcreatedb=t, rolcreaterole=t
```

**Verdict:** `nodi_admin` vẫn là superuser — đúng yêu cầu.

---

### Test 4: API health hoạt động ✅ PASS

```
Command: curl -sk https://localhost/api/health

Result: {"status":"ok","uptime":501,"version":"0.1.0"}
```

**Verdict:** API healthy, version 0.1.0.

---

### Test 5: `nodi_app` có quyền SELECT (CRUD read) ✅ PASS

```
Command: SELECT count(*) FROM accounts;

Result:
 count
-------
     5
(1 row)
```

**Verdict:** `nodi_app` có thể SELECT dữ liệu — quyền đọc hoạt động.

---

### Test 6: `nodi_app` KHÔNG có quyền superuser ✅ PASS

**Test 6a — CREATE TABLE (schema public):**
```
Command: CREATE TABLE _qa_test_sudo (id int);
Result:  ERROR: permission denied for schema public
```
→ `nodi_app` không có quyền CREATE trong schema public — **đúng yêu cầu, thậm chí chặt hơn kỳ vọng**.

**Test 6b — pg_terminate_backend (superuser-only):**
```
Command: SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE pid <> pg_backend_pid();
Result:  ERROR: permission denied to terminate process
         DETAIL: Only roles with the SUPERUSER attribute may terminate processes of roles with the SUPERUSER attribute.
```
→ `nodi_app` bị denied khi cố terminate connections — **chính xác**.

---

## Part B — Nginx Rate Limiting (#21)

### Test 7: `nginx -t` thành công ✅ PASS

```
Result:
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

---

### Test 8: Có 3 `limit_req_zone` ✅ PASS

```
Command: grep "limit_req_zone" /etc/nginx/nginx.conf

Result:
    limit_req_zone $rate_limit_key zone=api_general:10m rate=30r/s;
    limit_req_zone $rate_limit_key zone=auth_strict:10m rate=5r/s;
    limit_req_zone $rate_limit_key zone=sync_zone:10m rate=10r/s;
```

**Verdict:** 3 zones đúng yêu cầu:
| Zone | Rate | Size |
|------|------|------|
| `api_general` | 30r/s | 10m |
| `auth_strict` | 5r/s | 10m |
| `sync_zone` | 10r/s | 10m |

---

### Test 9: Dùng `$http_cf_connecting_ip` (Cloudflare-aware) ✅ PASS

```
Command: grep "cf_connecting_ip" /etc/nginx/nginx.conf

Result:
    map $http_cf_connecting_ip $rate_limit_key {
        default $http_cf_connecting_ip;
```

**Verdict:** Rate limit key tự động dùng IP thật từ Cloudflare header.

---

### Test 10: `limit_req` trên auth endpoints ✅ PASS

```
Endpoints có auth_strict:
 - /api/login              → limit_req zone=auth_strict burst=10 nodelay
 - /api/login-with-license → limit_req zone=auth_strict burst=10 nodelay
 - /api/auth/register      → limit_req zone=auth_strict burst=10 nodelay
```

**Verdict:** Tất cả auth endpoints đều có rate limit `auth_strict`.

---

### Test 11: WebSocket KHÔNG bị rate limit ✅ PASS

```
Command: grep -A3 "ws/sync\|support/ws" /etc/nginx/conf.d/nodi.conf

Result:
    location /api/support/ws {
        proxy_pass http://nodi-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
    --
    location /ws/sync {
        proxy_pass http://nodi-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
```

**Verdict:** Không có `limit_req` trong block WebSocket — đúng yêu cầu.

---

### Test 12: Rate limit thực sự hoạt động (burst test) ✅ PASS

```
Command: for i in $(seq 1 100); do curl -sk -o /dev/null -w "%{http_code} " https://localhost/api/health; done

Result (trích):
200 200 200 ... 200 200 429 200 429 200 429 429 200 429 200 429 200
```

**Verdict:** 429 (Too Many Requests) xuất hiện sau khi vượt burst — rate limiting đang hoạt động thực tế.

---

## Cleanup

```
Command: DROP TABLE IF EXISTS _qa_test_sudo;
Result:  DROP TABLE (NOTICE: table "_qa_test_sudo" does not exist, skipping)
```

Bảng test đã không được tạo (bị denied ở Test 6a), cleanup hoàn tất.

---

## Tổng kết

| # | Test | Kết quả |
|---|------|---------|
| 1 | Role `nodi_app` tồn tại, không có superuser | ✅ PASS |
| 2 | API dùng `nodi_app` | ✅ PASS |
| 3 | `nodi_admin` vẫn superuser | ✅ PASS |
| 4 | API health OK | ✅ PASS |
| 5 | `nodi_app` có quyền SELECT | ✅ PASS |
| 6 | `nodi_app` bị denied superuser ops | ✅ PASS |
| 7 | `nginx -t` OK | ✅ PASS |
| 8 | 3 `limit_req_zone` configured | ✅ PASS |
| 9 | Cloudflare-aware (`$http_cf_connecting_ip`) | ✅ PASS |
| 10 | Auth endpoints có `auth_strict` | ✅ PASS |
| 11 | WebSocket không bị rate limit | ✅ PASS |
| 12 | Rate limit hoạt động (429 returned) | ✅ PASS |

> **Sprint 128: READY TO SHIP** 🚀
