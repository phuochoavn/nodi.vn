# 🧪 BÁO CÁO KIỂM THỬ PHASE 5B — VPS (Sprint 153-162)

**Ngày**: 2026-03-21  
**Phạm vi**: `/opt/nodi/api/src/` + Docker config  
**Phương pháp**: Read-only grep/view verification + production smoke tests

---

## Tóm tắt

- **Tổng items**: 33
- **PASS**: 31 ✅
- **FAIL**: 0 ❌
- **SKIP**: 1 ⏭️ (cargo check timeout)
- **NOTE**: 1 ⚠️ (containers Up < 1h do recent deploy)
- **Kết luận**: **PASS — Code-level implementation đầy đủ**

---

## Phase 1: Build

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 0 | `cargo check` 0 errors | ⏭️ SKIP | Timeout sau 30+ phút — compiler kẹt. Tuy nhiên container `nodi-api` UP và serve requests bình thường → binary đã build thành công trước đó |

---

## Sprint 153: RLS Policies (6 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 1 | RLS ENABLED trên 23 bảng synced_* | ✅ PASS | `db.rs:342` — `ALTER TABLE {} ENABLE ROW LEVEL SECURITY` trong loop qua `synced_tables` array (23 entries, db.rs:191-215) |
| 2 | FORCE RLS trên 23 bảng | ✅ PASS | `db.rs:346` — `ALTER TABLE {} FORCE ROW LEVEL SECURITY` cùng loop |
| 3 | Policy tenant_isolation tồn tại | ✅ PASS | `db.rs:350` — `DROP POLICY IF EXISTS tenant_isolation` + `db.rs:357` — `CREATE POLICY tenant_isolation ON {}` |
| 4 | Policy dùng `current_setting('app.current_store_id')` | ✅ PASS | `db.rs:359-360` — `current_setting('app.current_store_id', true) = '' OR store_id = current_setting('app.current_store_id')::integer` |
| 5 | Graceful fallback khi chưa SET tenant | ✅ PASS | `db.rs:359` — `= ''` check cho trường hợp chưa SET (V1 handlers, direct pool queries) |
| 6 | 6 composite indexes (store_id, updated_at_v2 DESC) | ✅ PASS | `db.rs:369-374` — 6 indexes: `idx_invoices_store_updated`, `idx_invoice_items_store_updated`, `idx_product_tx_store_updated`, `idx_customer_tx_store_updated`, `idx_supplier_tx_store_updated`, `idx_purchase_orders_store_updated` |

---

## Sprint 154: TenantTransaction (6 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 7 | TenantContext struct có `store_id: i32` | ✅ PASS | `middleware/tenant.rs:17-20` — `pub struct TenantContext { pub store_id: i32, pub device_id: Option<String> }` |
| 8 | `set_tenant_on_tx()` dùng SET LOCAL | ✅ PASS | `middleware/tenant.rs:33` — `SET LOCAL app.current_store_id = '{}'` |
| 9 | `tenant_middleware()` function tồn tại | ✅ PASS | `middleware/tenant.rs:49` — `pub async fn tenant_middleware(...)` |
| 10 | Sync V2 handlers dùng `Extension<TenantContext>` | ✅ PASS | `sync_v2.rs:72,172,250` — 3 handlers đều có `Extension(ctx): Extension<TenantContext>` |
| 11 | V2 routes wrapped với tenant middleware | ✅ PASS | `main.rs:121,126` — `// V2 sync routes — with tenant middleware` + `middleware::tenant::tenant_middleware` |
| 12 | merge_engine gọi SET LOCAL sau begin() | ✅ PASS | `merge_engine.rs:67` — `crate::middleware::tenant::set_tenant_on_tx(&mut tx, store_id).await?;` |

---

## Sprint 155: PgBouncer (5 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 13 | PgBouncer service trong docker-compose.yml | ✅ PASS | `docker-compose.yml:51-71` — `pgbouncer:` service, image `edoburu/pgbouncer:latest`, container `nodi-pgbouncer` |
| 14 | `pool_mode = transaction` | ✅ PASS | `docker-compose.yml:59` — `POOL_MODE: transaction` |
| 15 | DATABASE_URL qua pgbouncer | ✅ PASS | `.env:6` — `APP_DATABASE_URL=postgres://nodi_app:...@pgbouncer:5432/nodi` (hostname = `pgbouncer`, không `postgres`) |
| 16 | max_connections > 10 | ✅ PASS | `db.rs:6` — `.max_connections(100)` (100 > 10) |
| 17 | PostgreSQL KHÔNG expose port ngoài docker | ✅ PASS | `docker-compose.yml:43-45` — ports commented out: `# ports:` / `#   - "127.0.0.1:5432:5432"` |

---

## Sprint 158: Protobuf VPS (4 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 18 | prost crate trong Cargo.toml | ✅ PASS | `Cargo.toml:23` — `prost = "0.13"` |
| 19 | Proto structs tồn tại | ✅ PASS | `models/proto.rs` — 9 structs with `#[derive(prost::Message)]`: SyncPushRequest, ChangeRow, SyncPushResponse, etc. |
| 20 | handle_push accept Content-Type negotiation | ✅ PASS | `sync_v2.rs:61` — response header `application/x-protobuf` |
| 21 | handle_pull/snapshot accept Accept header | ✅ PASS | `sync_v2.rs:47-49` — `fn accepts_protobuf(headers)` checks `accept` header; called at lines 78, 177, 255 (push, pull, snapshot) |

---

## Sprint 160: pgBackRest (3 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 22 | Dockerfile.postgres có pgbackrest | ✅ PASS | `Dockerfile.postgres:5` — `RUN apk add --no-cache pgbackrest` + lines 7-13: directories + config copy |
| 23 | postgresql.conf WAL archiving | ✅ PASS | `data/postgresql.conf:41-44` — `archive_mode = on`, `archive_command = 'pgbackrest --stanza=nodi_cluster archive-push %p'`, `archive_timeout = 300` |
| 24 | backup-cron.sh tồn tại | ✅ PASS | `scripts/backup-cron.sh` — 16 lines, supports `incr`/`full` backup types via pgbackrest |

---

## Sprint 161-162: Onboarding (6 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 25 | POST /api/stores/register endpoint | ✅ PASS | `onboarding.rs:17` — `.route("/api/stores/register", post(register_store))` |
| 26 | activation_key generation (6-char) | ✅ PASS | `onboarding.rs:26-29` — `fn generate_activation_key()` returns 6 random chars from `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` (no ambiguous 0/O/1/I) |
| 27 | Seed data nông nghiệp cho store mới | ✅ PASS | `onboarding.rs:148-158` — 8 categories: Phân bón, Thuốc BVTV, Giống cây trồng, Thức ăn chăn nuôi, Thuốc thú y, Dụng cụ nông nghiệp, Vật tư tưới tiêu, Khác |
| 28 | POST /api/devices/register endpoint | ✅ PASS | `onboarding.rs:18` — `.route("/api/devices/register", post(register_device))` |
| 29 | JWT issued chứa store_id claim | ✅ PASS | `onboarding.rs:235-236` — `auth::create_token(store_id, store_id, "device", ...)`. `auth.rs:10,26` — Claims struct has `pub store_id: i32` |
| 30 | Onboarding routes registered trong main.rs | ✅ PASS | `main.rs:120` — `.merge(routes::onboarding::router())` |

---

## Production Smoke (3 mục)

| # | Tiêu chí | Status | Bằng chứng |
|:-:|----------|:------:|------------|
| 31 | docker-compose ps — tất cả containers Up | ⚠️ NOTE | 5 containers Up: `nodi-api` (44min), `nodi-pgbouncer` (44min), `nodi-web` (48min), `nodi-postgres` (48min), `nodi-nginx` (29h). Uptime < 1h do recent deploy — không phải restart loop |
| 32 | API health → OK | ✅ PASS | Internal: `{"status":"ok","uptime":2717,"version":"0.1.0"}` HTTP 200, 4.5ms. External: HTTP 200, 142ms |
| 33 | V1 sync endpoints không 404 | ✅ PASS | `curl /api/sync/pull` → `{"error":true,"message":"Missing Authorization or X-HWID header"}` HTTP 401 — endpoint tồn tại, trả auth error (không 404/500) |

---

## Ghi chú bổ sung

### PgBouncer password exposure
> [!WARNING]
> `docker-compose.yml:56` — PgBouncer `DB_PASSWORD` hardcoded trực tiếp trong file thay vì dùng `${ENV_VAR}`. Nên chuyển sang env var.

### Container count
Docker compose có 5 services (thêm `pgbouncer` so với 4 cũ): nginx, postgres, pgbouncer, axum-api, nuxt-web.

### synced_tables array — 23 bảng
```
synced_customers, synced_products, synced_invoices, synced_invoice_items,
synced_invoice_payments, synced_suppliers, synced_purchase_orders, synced_purchase_items,
synced_customer_transactions, synced_supplier_transactions, synced_cash_transactions,
synced_product_units, synced_product_batches, synced_product_transactions,
synced_payment_vouchers, synced_store_settings, synced_promotions, synced_vouchers,
synced_daily_closings, synced_returns, synced_return_items,
synced_loyalty_transactions_v2, synced_store_funds
```
